import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';
import { RouterModule } from '@angular/router';
import { ReactiveFormsModule } from '@angular/forms';
import { SharedModule } from '../../shared/shared.module';
import { PaymentDetailNextComponent } from './paymentDetailNext.component';

export const routes = [
  { path: '', component: PaymentDetailNextComponent, pathMatch: 'full' }
];

@NgModule({
  imports: [
    CommonModule,
    RouterModule.forChild(routes),
    ReactiveFormsModule,
    SharedModule
  ],
  declarations: [
    PaymentDetailNextComponent
  ]
})
export class PaymentDetailNextModule { }
